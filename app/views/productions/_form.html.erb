<%= form_for @production, :html => { :class => "form-horizontal production" } do |f| %>

  <% if @production.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@production.errors.count, "error") %> prohibited this production from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @production.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :quntity, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :quntity, :class => 'form-control' %>
    </div>
    <%=f.error_span(:quntity) %>
  </div>
  <div class="form-group">
    <%= f.label :unit, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :unit, :class => 'form-control' %>
    </div>
    <%=f.error_span(:unit) %>
  </div>
  <div class="form-group">
    <%= f.label :product_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :product_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:product_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                productions_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
